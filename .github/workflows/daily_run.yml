name: ETF Arbitrage Monitor

on:
  schedule:
    # 🌟 修改点：UTC 00:50 = 北京时间 08:50
    # 完美避开早高峰，且能坚持到下午发完定投日报
    - cron: '50 0 * * 1-5'

  # 允许手动点击按钮测试
  workflow_dispatch:

jobs:
  run_monitor:
    runs-on: ubuntu-latest

    # 设置超时时间为 365 分钟 (6小时05分)
    # 配合 Python 代码的自动退出机制
    timeout-minutes: 365

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run Monitor Script
        run: |
          python main.py
